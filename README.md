# Metro Rush

Endless runner in the style of Subway Surfers — 3 lanes, jump, roll, trains and coins.

```bash
npm install
npm run dev
```

Open the local URL Vite prints.

**Controls:** Left/Right (or A/D) switch lanes · Up / W / Space jump · Down / S roll. Swipe on touch screens.
